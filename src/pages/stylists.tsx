import SideBar from "@/components/SideBar";
import StylistContent from "@/components/stylists/Content";
import React from "react";

export default function stylists() {
  return (
    <div>
      <SideBar />
      <StylistContent />
    </div>
  );
}
