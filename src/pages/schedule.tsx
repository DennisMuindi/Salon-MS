import SideBar from "@/components/SideBar";
import ScheduleContent from "@/components/schedule/ScheduleMain";
import React from "react";

export default function schedule() {
  return (
    <div>
      <SideBar />
      <ScheduleContent />
    </div>
  );
}
